import { Component } from '../component.js';
import { Template } from './template.js';

export class RequestsComponent extends Component {
    constructor(DEBUG_MODE, appRouter, profileService) {
        super(DEBUG_mode, {}, {}, undefined, undefined)
    }

    init() {

    }
}
